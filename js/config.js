// Runtime frontend configuration for NetDown (used by GitHub Pages)
// Set the public backend base URL here after you deploy the backend.
// Example:
// window.NETDOWN_API_BASE = 'https://netdown-api.example.com';

// Empty string means use localhost when developing locally, or same origin when served.
window.NETDOWN_API_BASE = '';
